auto_dubbing_system/
│
├── config/                                 # Cấu hình tĩnh (Global, Secure keys)
├── deploy/                                 # Docker, venv, đóng gói ứng dụng
├── storage/                                # Trọng số model (Cache), Database, Project files
│
└── src/                                    # MÃ NGUỒN CHÍNH
    │
    ├── main.py                             # Entry-point khởi động App
    │
    ├── features/                           # [SỰ THAY ĐỔI CỐT LÕI - ÁP DỤNG VSA]
    │   │                                   # Mỗi folder là một tính năng độc lập hoàn toàn
    │   │
    │   ├── _shared_components/             # UI Components và Utils dùng chung cho các Slice
    │   │   ├── style_theme.py
    │   │   └── audio_waveform_widget.py
    │   │
    │   ├── generate_subtitles_only/        # Slice 1: Tính năng chỉ tạo phụ đề (Bóc băng)
    │   │   ├── ui.py                       # Giao diện tải video & hiển thị text của riêng slice này
    │   │   ├── use_case.py                 # Logic: Trỏ thẳng xuống core để gọi STT module
    │   │   ├── models.py                   # Lược đồ dữ liệu riêng (chỉ quan tâm Text & Time)
    │   │   └── srt_exporter.py             # File xuất đuôi .srt (logic đặc thù không ai dùng chung)
    │   │
    │   ├── voice_cloning_studio/           # Slice 2: Tính năng quản lý giọng đọc (Thu âm, Test)
    │   │   ├── ui.py                       # Giao diện thu âm và quản lý danh sách giọng
    │   │   ├── use_case.py                 # Logic: Gửi API gọi module TTS
    │   │   └── local_db_queries.py         # Quản lý file sample âm thanh vào SQLite
    │   │
    │   └── full_auto_dubbing/              # Slice 3: Tính năng lồng tiếng toàn trình (Phức tạp nhất)
    │       ├── ui.py                       # Màn hình chính (Dashboard) với Timeline Editor
    │       ├── use_case.py                 # Logic: Kích hoạt toàn bộ Pipeline
    │       ├── pipeline_builder.py         # Cấu hình luồng DAG (Video -> STT -> Dịch -> TTS -> Mix)
    │       └── models.py                   # Lược đồ Manifest hoàn chỉnh
    │
    ├── core/                               # [TẦNG NỀN TẢNG NGANG - KHÔNG ĐỔI]
    │   ├── orchestrator/                   # Quản lý luồng DAG
    │   │   ├── dag_executor.py
    │   │   └── pipeline_manager.py
    │   ├── state/                          # Quản lý trạng thái và Checkpoint
    │   └── resource/                       # Quản lý cấp phát VRAM/RAM (Hardware Scheduler)
    │
    ├── modules/                            # [TẦNG GIAO DIỆN HỢP ĐỒNG - KHÔNG ĐỔI]
    │   ├── base_module.py
    │   ├── media/                          # Demuxer, Muxer (FFmpeg)
    │   ├── asr/                            # Giao diện STT
    │   ├── translation/                    # Giao diện Dịch thuật
    │   ├── tts/                            # Giao diện Text-to-Speech
    │   └── audio_post/                     # Giao diện hậu kỳ trộn âm
    │
    ├── plugins/                            # [TẦNG CÔNG NGHỆ LÕI - KHÔNG ĐỔI]
    │   ├── base_plugin.py
    │   ├── asr/
    │   │   ├── whisperx_local/             # Cô lập bằng Docker/Venv, giao tiếp qua Adapter
    │   │   └── openai_whisper/
    │   ├── translation/
    │   │   └── deepl_api/
    │   └── tts/
    │       ├── coqui_xtts_local/
    │       └── elevenlabs_api/
    │
    └── shared/                             # [HẠ TẦNG DÙNG CHUNG - KHÔNG ĐỔI]
        ├── config_manager.py
        ├── event_bus.py                    # Gửi tiến trình từ Plugins lên UI của Features
        └── structured_logger.py