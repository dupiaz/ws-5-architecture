[project]
name = "srt-translator"
version = "2.0.0"
description = "Parallel SRT subtitle translator using AI-Box API"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "tkinterdnd2>=0.3.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
