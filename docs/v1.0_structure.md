auto_dubbing_system/
│
├── config/                                 # Thư mục chứa cấu hình tĩnh trước Runtime
│   ├── global_config.yaml                  # Cấu hình toàn cục (App name, Cache path, Log level...)
│   ├── pipeline_config.yaml                # Cấu hình mặc định cho các chuỗi DAG
│   └── secure_schemas.json                 # Định nghĩa cấu trúc mã hóa cho API Keys/Credentials
│
├── deploy/                                 # Tài liệu và kịch bản đóng gói/triển khai
│   ├── docker/                             # Các Dockerfile cho các Local Inference Plugins
│   │   ├── Dockerfile.whisperx             # Môi trường cô lập cho WhisperX (CUDA-enabled)
│   │   └── Dockerfile.xtts                 # Môi trường cô lập cho Coqui XTTS v2
│   ├── venv_builders/                      # Script tạo Virtual Environments riêng biệt nếu không dùng Docker
│   │   ├── setup_whisperx_env.bat
│   │   └── setup_xtts_env.bat
│   └── build_windows.py                    # Script đóng gói ứng dụng chính bằng PyInstaller/Nuget
│
├── storage/                                # Phân vùng lưu trữ dữ liệu cục bộ (Local Persistence)
│   ├── cache/                              # Unified Model Cache (Lưu trữ Model Weights tập trung)
│   │   ├── .hf_home/                       # Trỏ biến môi trường HF_HOME về đây (HuggingFace)
│   │   └── checkpoints/                    # Nơi lưu trữ trọng số tải thủ công từ ElevenLabs/Coqui
│   ├── db/
│   │   └── system_state.db                 # SQLite database phục vụ State Machine & Checkpointing
│   └── projects/                           # Thư mục lưu dữ liệu theo từng dự án của người dùng
│       └── project_sample_001/             # Ví dụ cấu trúc của một Project folder
│           ├── raw_video.mp4
│           ├── extracted_vocal.wav
│           ├── background_sfx.wav
│           ├── manifest.json               # Cấu trúc dữ liệu chuẩn hóa (Text, Timestamp, Status)
│           └── output_dubbed.mp4
│
└── src/                                    # Mã nguồn ứng dụng chính (Source Code)
    │
    ├── main.py                             # Entry-point duy nhất của toàn bộ hệ thống
    │
    ├── presentation/                       # 1. PRESENTATION LAYER
    │   ├── __init__.py
    │   ├── gui/                            # Desktop GUI Client (Sử dụng PySide6/PyQt6)
    │   │   ├── views/                      # Tầng hiển thị giao diện trực quan (UI)
    │   │   │   ├── main_window.py
    │   │   │   ├── project_dashboard.py
    │   │   │   └── timeline_editor.py      # Giao diện sửa text/waveform
    │   │   ├── viewmodels/                 # Tầng logic điều khiển giao diện (MVVM Pattern)
    │   │   │   ├── main_viewmodel.py
    │   │   │   └── timeline_viewmodel.py
    │   │   └── components/                 # Các UI components tái sử dụng
    │   │       └── waveform_widget.py
    │   ├── cli/                            # CLI Client (Dùng Click hoặc Argparse)
    │   │   ├── __init__.py
    │   │   └── commands.py                 # Định nghĩa các tập lệnh cho Automation/Batching
    │   └── api/                            # Local API Gateway (Nếu GUI tách process với Core)
    │       ├── __init__.py
    │       └── router.py                   # Tiếp nhận HTTP/WebSocket request nội bộ
    │
    ├── core/                               # 2. ORCHESTRATION & CONTROL PLANE
    │   ├── __init__.py
    │   ├── orchestrator/
    │   │   ├── __init__.py
    │   │   ├── dag_executor.py             # Bộ máy thực thi luồng công việc dưới dạng đồ thị (DAG)
    │   │   └── pipeline_manager.py         # Quản lý vòng đời và chuỗi tuần tự của dự án
    │   ├── state/
    │   │   ├── __init__.py
    │   │   ├── state_machine.py            # Quản lý trạng thái Task (Pending, Running, Failed...)
    │   │   └── checkpointer.py             # Cơ chế lưu điểm phục hồi và rollback dữ liệu khi lỗi
    │   └── resource/
    │       ├── __init__.py
    │       ├── hardware_scheduler.py       # Giám sát phần cứng thời gian thực (Real-time monitor)
    │       └── vram_manager.py             # Quản lý nạp/giải phóng (Load/Unload) Model Weights trên GPU
    │
    ├── modules/                            # 3. EXECUTION ENGINE & FUNCTIONAL MODULES
    │   ├── __init__.py
    │   ├── base_module.py                  # Abstract Base Class định nghĩa Interface Contract chung
    │   │
    │   ├── media/                          # Media Demuxing/Muxing Pipeline
    │   │   ├── __init__.py
    │   │   ├── demuxer.py                  # Tách Audio/Video/Subtitle bằng FFmpeg
    │   │   └── muxer.py                    # Hòa trộn (Merge) luồng audio mới vào video gốc
    │   │
    │   ├── asr/                            # Automatic Speech Recognition Engine
    │   │   ├── __init__.py
    │   │   └── asr_processor.py            # Tiếp nhận audio, điều phối việc bóc băng sang Plugin
    │   │
    │   ├── translation/                    # Machine Translation Engine
    │   │   ├── __init__.py
    │   │   └── translation_processor.py
    │   │
    │   ├── tts/                            # Text-to-Speech Engine
    │   │   ├── __init__.py
    │   │   └── tts_processor.py
    │   │
    │   └── audio_post/                     # Audio Post-Processing Engine
    │       ├── __init__.py
    │       ├── time_stretcher.py           # Thu/phóng độ dài âm thanh dựa trên timestamp mà không đổi cao độ
    │       └── mixer.py                    # Trộn giọng đọc mới với background noise/SFX
    │
    ├── plugins/                            # 4. PLUGIN ARCHITECTURE & ADAPTER LAYER
    │   ├── __init__.py
    │   ├── base_plugin.py                  # Định nghĩa Interface Contract cho tất cả các Plugin
    │   │
    │   ├── asr/
    │   │   ├── whisperx_local/             # Plugin chạy WhisperX nội bộ (Docker/Isolated venv)
    │   │   │   ├── __init__.py
    │   │   │   ├── adapter.py              # Áp dụng Adapter Pattern để map dữ liệu chuẩn hóa
    │   │   │   └── client.py               # Kết nối qua Local REST/gRPC đến container WhisperX
    │   │   └── openai_whisper/             # Plugin gọi Cloud API của OpenAI
    │   │       ├── __init__.py
    │   │       └── adapter.py              # Xử lý Rate Limiting, Retry Pattern
    │   │
    │   ├── translation/
    │   │   ├── deepl_api/
    │   │   │   ├── __init__.py
    │   │   │   └── adapter.py
    │   │   └── google_translate_api/
    │   │       ├── __init__.py
    │   │       └── adapter.py
    │   │
    │   └── tts/
    │       ├── coqui_xtts_local/           # Local Inference Service cho XTTS v2
    │       │   ├── __init__.py
    │       │   ├── adapter.py
    │       │   └── client.py
    │       └── elevenlabs_api/             # Cloud API cho ElevenLabs Voice Cloning
    │           ├── __init__.py
    │           └── adapter.py              # Gắn Circuit Breaker đề phòng đứt mạng hoặc hết hạn gói
    │
    └── shared/                             # 5. SHARED INFRASTRUCTURE
        ├── __init__.py
        ├── config/
        │   ├── __init__.py
        │   └── config_manager.py           # Bộ quản lý cấu hình phân cấp nạp ở Runtime
        ├── events/
        │   ├── __init__.py
        │   └── event_bus.py                # Unified Event Bus (Sử dụng ZeroMQ/In-Memory Pub-Sub)
        ├── logging/
        │   ├── __init__.py
        │   └── structured_logger.py        # Ghi nhật ký cấu hình dạng JSON tập trung
        └── utils/
            ├── __init__.py
            ├── file_system.py              # Xử lý an toàn đường dẫn Windows (Long paths, Symlinks)
            └── crypto.py                   # Mã hóa/Giải mã API keys lưu cục bộ